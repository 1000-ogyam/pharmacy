# Build Prompt: PL PharmaCore

The original implementation spec (formerly `readme.md`) is preserved here. Follow `README.md` for local setup.

See the conversation workspace copy if you need the full section-by-section prompt; this repository was generated from that document (custom MVC, single front controller, sequenced milestones).
